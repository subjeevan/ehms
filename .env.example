# Copy these values into your IDE run configuration, PowerShell session,
# system environment, Docker configuration, or deployment platform.
# Never commit real production secrets.

DB_URL=jdbc:mysql://localhost:3306/ehms?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Tokyo
DB_USERNAME=hms_user
DB_PASSWORD=replace-with-your-database-password
JWT_SECRET=replace-with-a-random-secret-of-at-least-32-characters
JWT_EXPIRATION_MINUTES=120
CORS_ALLOWED_ORIGINS=http://localhost:3000
SERVER_PORT=8080
LOG_FILE=logs/ehms.log
